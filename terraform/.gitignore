# Terraform state files
*.tfstate
*.tfstate.*
crash.log

# Terraform data directory
.terraform/

# Terraform variable definitions (potentially containing sensitive data)
*.tfvars
*.tfvars.json

# Terraform CLI configuration files
.terraformrc
terraform.rc

# Local override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json


# Terraform plan
*.tfplan

# ignore all unencrypted secrets
*secrets.*
*decrypted.*
secrets.*
decrypted.*

# Allow encrypted secrets to be tracked
!*.encrypted.*
!*secrets.encrypted.*